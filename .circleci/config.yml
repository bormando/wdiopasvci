version: 2.1

orbs:
  node: circleci/node@4.1


jobs:
  build-and-test:
    docker:
      - image: circleci/node:latest-browsers
    steps:
      - checkout
      - node/install-packages
      - run:
          name: Run tests
          command: |
            wget $(curl https://circleci.com/api/v1.1/project/github/bormando/wdiopasvci/latest/artifacts?filter=successful -H "Circle-Token $CIRCLE_TOKEN" | grep -Eo '(http|https)://[a-zA-Z0-9./?=_%:-]*') -O allure-previous.zip
            test -f allure-previous.zip && unzip allure-previous.zip -d allure-previous
            npm run test:chrome:headless
            cp -r allure-previous/allure-report/history allure-results
            npm run report:generate
            zip -r allure-report.zip allure-report
      - run:
          when: on_fail
          name: Generate fail results
          command: |
            wget $(curl https://circleci.com/api/v1.1/project/github/bormando/wdiopasvci/latest/artifacts?filter=successful -H "Circle-Token $CIRCLE_TOKEN" | grep -Eo '(http|https)://[a-zA-Z0-9./?=_%:-]*') -O allure-previous.zip
            test -f allure-previous.zip && unzip allure-previous.zip -d allure-previous
            cp -r allure-previous/allure-report/history allure-results
            npm run report:generate
            zip -r allure-results.zip allure-results
      - store_artifacts:
          path: allure-report.zip

workflows:
  commit:
    jobs:
      - build-and-test
  nightly:
    triggers:
      - schedule:
          cron: "0 0 * * *"
          filters:
            branches:
              only:
                - master
    jobs:
      - build-and-test

